// import { PrismaClient, Prisma, Camera } from "@prisma/client"

// export default class CameraRepository {

//     public async create(prisma:PrismaClient, newCamera: Prisma.CameraCreateInput): Promise<Camera>{
//         const createdCamera = await prisma.camera.create({
//             data: newCamera
//         })

//         return createdCamera
//     }

//     public static createMany(){
        
//     }
// }